# Schema Registry & Protobuf CI
# Ticket: RIP-1-007

name: Schema Registry

on:
  push:
    branches: [main]
    paths:
      - "packages/proto/**"
  pull_request:
    branches: [main]
    paths:
      - "packages/proto/**"

permissions:
  contents: read

jobs:
  buf:
    name: buf lint + breaking + generate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: bufbuild/buf-setup-action@v1

      - name: buf lint
        working-directory: packages/proto
        run: buf lint

      - name: buf breaking (against main)
        working-directory: packages/proto
        run: buf breaking --against 'https://github.com/${{ github.repository }}.git#branch=main,subdir=packages/proto'

      - name: buf generate
        working-directory: packages/proto
        run: buf generate

      - name: Verify TypeScript codegen output
        run: test -d packages/proto/gen/ts

      - name: buf push (BSR)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          BUF_TOKEN: ${{ secrets.BUF_TOKEN }}
        run: |
          if [ -z "$BUF_TOKEN" ]; then
            echo "BUF_TOKEN not set — skipping buf push (add secret to enable Schema Registry sync)"
            exit 0
          fi
          buf push packages/proto
