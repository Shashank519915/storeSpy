# @rip/proto

Protobuf schemas for RIP event backbone. Governed by `buf.yaml` with `DEFAULT` lint and `FILE` breaking rules.

## Commands

```bash
pnpm proto:lint
pnpm proto:generate
```

Generated Go/Python code is CI-only. TypeScript generated code may be committed for portal consumption.
