# @rip/ui

Shared Radix + Tailwind component library and design tokens for the RIP portal.

## Tokens

All visual values trace to `tokens/*.css` per `design-tokens.md`. Import via:

```css
@import "@rip/ui/tokens";
```

Never use raw hex or ad-hoc Tailwind color literals in components.
