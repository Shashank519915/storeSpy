# Istio — STRICT mTLS default-deny
# Ticket: RIP-0-030

apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: STRICT

---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: rip-system-strict
  namespace: rip-system
spec:
  mtls:
    mode: STRICT
